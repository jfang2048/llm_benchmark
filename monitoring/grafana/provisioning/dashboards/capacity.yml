apiVersion: 1

providers:
  - name: inference-capacity
    orgId: 1
    folder: LLM Infrastructure
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    allowUiUpdates: false

    options:
      path: /var/lib/grafana/dashboards
